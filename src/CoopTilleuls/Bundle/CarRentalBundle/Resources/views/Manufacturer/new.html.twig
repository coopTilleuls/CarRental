{% extends 'DunglasCarRentalBundle::layout.html.twig' %}

{% block content %}
<h1>Manufacturer creation</h1>

<form action="{{ path('manufacturer_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('manufacturer') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}