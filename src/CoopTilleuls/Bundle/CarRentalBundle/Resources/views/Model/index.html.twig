{% extends 'DunglasCarRentalBundle::layout.html.twig' %}

{% block content %}
<h1>Model list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Manufacturer</th>
            <th>Name</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('model_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.manufacturer }}</td>
            <td>{{ entity.name }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('model_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('model_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('model_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
{% endblock %}