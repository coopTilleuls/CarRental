{% extends 'DunglasCarRentalBundle::layout.html.twig' %}

{% block content %}
<h1>Vehicle creation</h1>

<form action="{{ path('vehicle_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('vehicle') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}